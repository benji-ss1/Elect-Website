You can run the code 2 ways, by either opening the zip and extracting all the content. 
Use VS Code, for example, to see the code and run it.
You can use the separate HTML files and put them in a folder with the photos, and run them that way within VS Code as well.
We built this as a surprise mock-up for a clothing brand who are currently revamping.
